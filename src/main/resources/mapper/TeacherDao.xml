<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.thesisbuddy.dao.TeacherDao">
    
    <!-- 结果映射 -->
    <resultMap id="TeacherAccountResultMap" type="org.example.thesisbuddy.entity.TeacherAccount">
        <id property="teacherId" column="teacher_id"/>
        <result property="account" column="account"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="campusId" column="campus_id"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
    
    <!-- 根据工号查询教师 -->
    <select id="selectByAccount" resultMap="TeacherAccountResultMap">
        SELECT teacher_id, account, password_hash, real_name, phone, campus_id, title, status, create_time
        FROM teacher_account
        WHERE account = #{account}
    </select>
    
    <!-- 根据教师ID查询 -->
    <select id="selectById" resultMap="TeacherAccountResultMap">
        SELECT teacher_id, account, password_hash, real_name, phone, campus_id, title, status, create_time
        FROM teacher_account
        WHERE teacher_id = #{teacherId}
    </select>
    
    <!-- 插入教师 -->
    <insert id="insert">
        INSERT INTO teacher_account(account, password_hash, real_name, campus_id, title, status, create_time)
        VALUES(#{account}, #{passwordHash}, #{realName}, #{campusId}, #{title}, #{status}, NOW())
    </insert>
    
    <!-- 更新教师状态 -->
    <update id="updateStatus">
        UPDATE teacher_account
        SET status = #{status}
        WHERE teacher_id = #{teacherId}
    </update>
    
    <!-- 查询所有教师 -->
    <select id="selectAll" resultMap="TeacherAccountResultMap">
        SELECT teacher_id, account, password_hash, real_name, phone, campus_id, title, status, create_time
        FROM teacher_account
    </select>
    
    <!-- 统计教师总数 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM teacher_account
    </select>
</mapper>
